[task]
name = "mvcc-gc-repair"
category = "database_engineering"
difficulty = "hard"

[task.description]
summary = "Debug an MVCC garbage collector that incorrectly identifies versions for collection"
domain = "Multi-Version Concurrency Control garbage collection for a versioned key-value store"

[docker]
dockerfile = "environment/Dockerfile"
context = "environment"

[tests]
script = "tests/test.sh"
total = 15
passing_with_bugs = 7

[solution]
script = "solution/solve.sh"
