[scheduler]
priority_levels = high,medium,low, critical
max_concurrent = 20
default_timeout = 60
retry_limit = 3
execution_mode = parallel

[scheduler.limits]
max_concurrent = 5
max_rounds = 50
time_budget = 300.0

[scheduler.logging]
level = INFO
format = structured
